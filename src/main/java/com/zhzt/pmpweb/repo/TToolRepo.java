package com.zhzt.pmpweb.repo;

import com.zhzt.pmpweb.entity.TTool;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TToolRepo extends JpaRepository<TTool, Integer> {
}
